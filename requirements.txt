# Local experimentation. Kaggle notebooks already have all of this.
numpy>=1.24
pandas>=2.0
pyarrow>=12.0
scipy>=1.10

# chemistry — pinned to the version the competition grades with
rdkit==2026.3.3

# notebooks + kaggle sync
nbformat>=5.9
jupyter
kaggle>=1.6

# optional, for modelling work
scikit-learn>=1.3
matplotlib>=3.7
tqdm
